# IntelliNews Backend Environment Configuration

# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration
# For Development (SQLite) - leave DATABASE_URL empty or unset
DB_PATH=./data.db

# For Production (PostgreSQL) - set this to enable PostgreSQL
# DATABASE_URL=postgresql://username:password@host:port/database
# Example for local PostgreSQL:
# DATABASE_URL=postgresql://intellinews:password@localhost:5432/intellinews_db
# Example for cloud PostgreSQL (like Railway, Supabase, etc.):
# DATABASE_URL=postgresql://user:pass@host.railway.app:5432/railway

# AI Provider Configuration
# Options: mock, openai, gemini
AI_PROVIDER=mock

# OpenAI Configuration (if using AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini Configuration (if using AI_PROVIDER=gemini)  
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Custom prompts file location
# PROMPTS_FILE=./config/prompts.json

# Security & Performance
# CORS_ORIGIN=http://localhost:3000
# LOG_LEVEL=info