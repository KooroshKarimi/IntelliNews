name: Build and Deploy to Cloud Run

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read      # read-only access to the repo
      id-token: write     # required for Workload Identity/OIDC

    steps:
      - uses: actions/checkout@v4

      # Authenticate to Google Cloud using the JSON key stored in the secret
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'
          token_format: 'access_token'

      # Build the container image and deploy/update Cloud Run in one step
      - id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: backend                     # Cloud Run service name
          region: ${{ secrets.GCP_REGION }}    # e.g. europe-west3
          source: .                            # directory containing Dockerfile
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          allow_unauthenticated: true